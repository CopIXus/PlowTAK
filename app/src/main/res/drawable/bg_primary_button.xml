<?xml version="1.0" encoding="utf-8"?>
<!-- Primary CTA: dark fill + manatee border (ATAK Tools-like). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/plowtak_panel" />
    <stroke
        android:width="2dp"
        android:color="@color/plowtak_tile_bar" />
    <corners android:radius="4dp" />
</shape>
